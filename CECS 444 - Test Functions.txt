//Test functions

//Test #1
prog main { print( "ASCII:", " A= ", 65, " Z= ", 90 ); }

//Test #2
prog main { // Find the circumgerence of a circle.
	pi = 3.14;
	print( "Input radius> ");
	rx = input ( float );
	circum = 2 * pi * rx;
	print( "Circumf= ", circum );
}

//Test #3
prog main { //Find the hypotenuse of a right triangle.
	print( "Input legs> " );
	a = input( int );
	b = input( int) ;
	print( "Hypoentuse= ", ( a * a + b * b ) ^ 0.5 );
}
